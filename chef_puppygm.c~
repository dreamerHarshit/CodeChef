#include<stdio.h>

int main()
{
	long long int t,i,a,b;
	
	scanf("%lld",&t);
	
	while(t--)
	{
		scanf("%lld%lld",&a,&b);
	
		if(a%2!=0&&b%2!=0)
		 {
		 	printf("Vanka\n");
		 }
		else
				printf("Tuzik\n");
	}


return 0;
}
